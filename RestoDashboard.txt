'use client';

import React, { useState } from 'react';
import {
  Menu,
  X,
  ChefHat,
  BarChart3,
  MessageSquare,
  Package,
  Grid3X3,
  Calendar,
  Mic2,
  Users,
  MapPin,
  Home,
  ChevronDown,
  Wifi,
  WifiOff,
  Plus,
  TrendingUp,
  Clock,
  AlertTriangle,
  ArrowUpRight,
  ArrowDownRight,
} from 'lucide-react';

export default function RestoDashboard() {
  const [sidebarOpen, setSidebarOpen] = useState(true);
  const [isOnline, setIsOnline] = useState(true);
  const [selectedBranch, setSelectedBranch] = useState('Main Branch - Shirdi');

  // Module definitions
  const modules = [
    { id: 1, name: 'CRM Dashboard', icon: Home, color: 'text-blue-500' },
    { id: 2, name: 'Sales Reports', icon: BarChart3, color: 'text-emerald-500' },
    { id: 3, name: 'WhatsApp Billing', icon: MessageSquare, color: 'text-green-500' },
    { id: 4, name: 'Inventory Control', icon: Package, color: 'text-amber-500' },
    { id: 5, name: 'Table Manager', icon: Grid3X3, color: 'text-purple-500' },
    { id: 6, name: 'Event Scheduler', icon: Calendar, color: 'text-pink-500' },
    { id: 7, name: 'Voice KOT', icon: Mic2, color: 'text-orange-500' },
    { id: 8, name: 'Staff Roster', icon: Users, color: 'text-indigo-500' },
    { id: 9, name: 'Multi-Location', icon: MapPin, color: 'text-red-500' },
    { id: 10, name: 'Central Dashboard', icon: ChefHat, color: 'text-yellow-500' },
  ];

  // Sample KPI Data
  const kpis = [
    {
      title: "Today's Sales",
      value: '₹45,230',
      change: '+12.5%',
      isPositive: true,
      icon: TrendingUp,
      bgColor: 'from-blue-50 to-blue-100',
      borderColor: 'border-blue-300',
    },
    {
      title: 'Active Tables',
      value: '12',
      change: '5 available',
      isPositive: true,
      icon: Grid3X3,
      bgColor: 'from-emerald-50 to-emerald-100',
      borderColor: 'border-emerald-300',
    },
    {
      title: 'Pending KOTs',
      value: '8',
      change: '2 urgent',
      isPositive: false,
      icon: Clock,
      bgColor: 'from-orange-50 to-orange-100',
      borderColor: 'border-orange-300',
    },
    {
      title: 'Low Stock Alerts',
      value: '3',
      change: 'Paneer, Chicken',
      isPositive: false,
      icon: AlertTriangle,
      bgColor: 'from-red-50 to-red-100',
      borderColor: 'border-red-300',
    },
  ];

  // Sample Recent Orders Data
  const recentOrders = [
    {
      id: 'KOT-2024-001',
      table: 'Table 5',
      items: 'Paneer Tikka, Dal Makhani, Naan (2)',
      amount: '₹450',
      time: '2:15 PM',
      status: 'Ready',
      statusColor: 'bg-emerald-100 text-emerald-800',
    },
    {
      id: 'KOT-2024-002',
      table: 'Table 8',
      items: 'Chole Bhature, Lassi',
      amount: '₹320',
      time: '2:05 PM',
      status: 'Preparing',
      statusColor: 'bg-amber-100 text-amber-800',
    },
    {
      id: 'KOT-2024-003',
      table: 'Table 2',
      items: 'Maharashtrian Thali, Chaas',
      amount: '₹380',
      time: '1:55 PM',
      status: 'Served',
      statusColor: 'bg-blue-100 text-blue-800',
    },
    {
      id: 'KOT-2024-004',
      table: 'Table 12',
      items: 'Biryani, Raita, Dessert',
      amount: '₹520',
      time: '1:40 PM',
      status: 'Preparing',
      statusColor: 'bg-amber-100 text-amber-800',
    },
    {
      id: 'KOT-2024-005',
      table: 'Table 1',
      items: 'Samosa (4), Chai',
      amount: '₹180',
      time: '1:25 PM',
      status: 'Ready',
      statusColor: 'bg-emerald-100 text-emerald-800',
    },
  ];

  return (
    <div className="flex h-screen bg-gray-50">
      {/* SIDEBAR */}
      <aside
        className={`${
          sidebarOpen ? 'w-64' : 'w-20'
        } bg-[#0B2545] text-white transition-all duration-300 flex flex-col shadow-lg overflow-y-auto`}
      >
        {/* Logo & Tagline */}
        <div className="p-6 border-b border-blue-900">
          <div className="flex items-center gap-3 mb-3">
            <ChefHat className="w-8 h-8 text-[#FF6B00]" />
            {sidebarOpen && (
              <div>
                <h1 className="text-xl font-bold text-white">RestoSaathi</h1>
                <p className="text-xs text-gray-400">Billing. Operations. Growth.</p>
              </div>
            )}
          </div>
        </div>

        {/* Navigation Links */}
        <nav className="flex-1 px-4 py-6 space-y-2">
          {modules.map((module) => {
            const IconComponent = module.icon;
            return (
              <button
                key={module.id}
                className={`w-full flex items-center gap-4 px-4 py-3 rounded-lg transition-all duration-200 ${
                  module.id === 10
                    ? 'bg-[#FF6B00] text-white shadow-lg'
                    : 'hover:bg-blue-800 text-gray-200 hover:text-white'
                }`}
                title={module.name}
              >
                <IconComponent className="w-5 h-5 flex-shrink-0" />
                {sidebarOpen && <span className="text-sm font-medium">{module.name}</span>}
              </button>
            );
          })}
        </nav>

        {/* Sidebar Toggle */}
        <button
          onClick={() => setSidebarOpen(!sidebarOpen)}
          className="mx-4 mb-6 w-12 h-12 flex items-center justify-center bg-blue-800 hover:bg-blue-700 rounded-lg transition-colors"
          title={sidebarOpen ? 'Collapse sidebar' : 'Expand sidebar'}
        >
          {sidebarOpen ? <X className="w-5 h-5" /> : <Menu className="w-5 h-5" />}
        </button>
      </aside>

      {/* MAIN CONTENT */}
      <div className="flex-1 flex flex-col overflow-hidden">
        {/* TOP BAR */}
        <header className="bg-white border-b border-gray-200 shadow-sm">
          <div className="px-8 py-4 flex items-center justify-between">
            {/* Left: Branch Selector */}
            <div className="flex items-center gap-2">
              <div className="relative">
                <button className="flex items-center gap-2 px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-lg transition-colors text-gray-700 font-medium">
                  <MapPin className="w-4 h-4" />
                  {selectedBranch}
                  <ChevronDown className="w-4 h-4" />
                </button>
              </div>
            </div>

            {/* Right: Status & Actions */}
            <div className="flex items-center gap-6">
              {/* Status Badge */}
              <div className="flex items-center gap-2">
                <button
                  onClick={() => setIsOnline(!isOnline)}
                  className={`flex items-center gap-2 px-4 py-2 rounded-lg font-medium transition-all ${
                    isOnline
                      ? 'bg-emerald-100 text-emerald-700 hover:bg-emerald-200'
                      : 'bg-red-100 text-red-700 hover:bg-red-200'
                  }`}
                >
                  {isOnline ? (
                    <Wifi className="w-4 h-4" />
                  ) : (
                    <WifiOff className="w-4 h-4" />
                  )}
                  {isOnline ? 'Online' : 'Offline'}
                </button>
              </div>

              {/* New KOT Button */}
              <button className="flex items-center gap-2 px-6 py-2.5 bg-[#FF6B00] hover:bg-orange-700 text-white font-semibold rounded-lg transition-all shadow-md hover:shadow-lg transform hover:scale-105">
                <Plus className="w-5 h-5" />
                New KOT
              </button>
            </div>
          </div>
        </header>

        {/* DASHBOARD CONTENT */}
        <main className="flex-1 overflow-auto p-8">
          {/* KPI Cards */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            {kpis.map((kpi, index) => {
              const IconComponent = kpi.icon;
              return (
                <div
                  key={index}
                  className={`bg-gradient-to-br ${kpi.bgColor} border-2 ${kpi.borderColor} rounded-xl p-6 shadow-md hover:shadow-lg transition-all`}
                >
                  <div className="flex justify-between items-start mb-4">
                    <h3 className="text-gray-600 font-medium text-sm">{kpi.title}</h3>
                    <div className="bg-white rounded-lg p-2 shadow-sm">
                      <IconComponent className="w-5 h-5 text-gray-700" />
                    </div>
                  </div>
                  <div className="mb-3">
                    <p className="text-3xl font-bold text-gray-800">{kpi.value}</p>
                  </div>
                  <div
                    className={`flex items-center gap-1 text-sm font-semibold ${
                      kpi.isPositive ? 'text-emerald-600' : 'text-red-600'
                    }`}
                  >
                    {kpi.isPositive ? (
                      <ArrowUpRight className="w-4 h-4" />
                    ) : (
                      <ArrowDownRight className="w-4 h-4" />
                    )}
                    {kpi.change}
                  </div>
                </div>
              );
            })}
          </div>

          {/* Recent Orders Section */}
          <div className="bg-white rounded-xl shadow-md border border-gray-200">
            <div className="px-6 py-4 border-b border-gray-200">
              <h2 className="text-xl font-bold text-gray-800">Recent Orders</h2>
            </div>

            {/* Table */}
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead>
                  <tr className="bg-gray-50 border-b border-gray-200">
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      KOT ID
                    </th>
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      Table
                    </th>
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      Items
                    </th>
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      Amount
                    </th>
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      Time
                    </th>
                    <th className="px-6 py-3 text-left text-xs font-semibold text-gray-600">
                      Status
                    </th>
                  </tr>
                </thead>
                <tbody>
                  {recentOrders.map((order, index) => (
                    <tr
                      key={index}
                      className="border-b border-gray-100 hover:bg-gray-50 transition-colors"
                    >
                      <td className="px-6 py-4 text-sm font-semibold text-gray-800">
                        {order.id}
                      </td>
                      <td className="px-6 py-4 text-sm text-gray-700">{order.table}</td>
                      <td className="px-6 py-4 text-sm text-gray-600">{order.items}</td>
                      <td className="px-6 py-4 text-sm font-semibold text-gray-800">
                        {order.amount}
                      </td>
                      <td className="px-6 py-4 text-sm text-gray-600">{order.time}</td>
                      <td className="px-6 py-4">
                        <span
                          className={`inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold ${order.statusColor}`}
                        >
                          {order.status}
                        </span>
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>

            {/* Table Footer */}
            <div className="px-6 py-4 border-t border-gray-200 flex justify-between items-center">
              <p className="text-sm text-gray-600">Showing 5 of 24 orders</p>
              <button className="px-4 py-2 text-[#FF6B00] font-semibold hover:bg-orange-50 rounded-lg transition-colors">
                View All Orders →
              </button>
            </div>
          </div>
        </main>
      </div>
    </div>
  );
}