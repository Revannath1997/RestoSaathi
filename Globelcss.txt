@tailwind base;
@tailwind components;
@tailwind utilities;

/* RestoSaathi Custom Styles */

/* Root Color Scheme */
:root {
  --color-navy: #0B2545;
  --color-orange: #FF6B00;
  --color-slate: #1E293B;
  --color-success: #22C55E;
  --color-danger: #EF4444;
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

/* Typography */
@layer base {
  body {
    @apply bg-gray-50 text-gray-900;
  }

  h1 {
    @apply text-2xl font-bold text-gray-900;
  }

  h2 {
    @apply text-xl font-bold text-gray-800;
  }

  h3 {
    @apply text-lg font-semibold text-gray-800;
  }

  button {
    @apply transition-all duration-200;
  }

  input,
  select,
  textarea {
    @apply rounded-lg border border-gray-300 px-4 py-2 focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 focus:outline-none;
  }
}

/* Component Styles */
@layer components {
  /* Sidebar Styles */
  .sidebar {
    @apply w-64 bg-[#0B2545] text-white shadow-lg;
  }

  .sidebar-collapsed {
    @apply w-20;
  }

  .sidebar-link {
    @apply flex items-center gap-4 px-4 py-3 rounded-lg transition-all duration-200;
  }

  .sidebar-link-active {
    @apply bg-[#FF6B00] text-white shadow-lg;
  }

  .sidebar-link-hover {
    @apply hover:bg-blue-800 text-gray-200 hover:text-white;
  }

  /* Header Styles */
  .header {
    @apply bg-white border-b border-gray-200 shadow-sm sticky top-0 z-40;
  }

  .header-content {
    @apply px-8 py-4 flex items-center justify-between;
  }

  /* Button Styles */
  .btn {
    @apply px-4 py-2 rounded-lg font-medium transition-all duration-200;
  }

  .btn-primary {
    @apply bg-[#FF6B00] hover:bg-orange-700 text-white shadow-md hover:shadow-lg;
  }

  .btn-secondary {
    @apply bg-gray-100 hover:bg-gray-200 text-gray-700;
  }

  .btn-ghost {
    @apply hover:bg-gray-100 text-gray-700;
  }

  .btn-lg {
    @apply px-6 py-3 text-lg font-semibold;
  }

  .btn-sm {
    @apply px-3 py-1.5 text-sm;
  }

  /* Card Styles */
  .card {
    @apply bg-white rounded-xl shadow-md border border-gray-200;
  }

  .card-header {
    @apply px-6 py-4 border-b border-gray-200;
  }

  .card-body {
    @apply p-6;
  }

  .card-footer {
    @apply px-6 py-4 border-t border-gray-200;
  }

  /* KPI Card Styles */
  .kpi-card {
    @apply bg-gradient-to-br rounded-xl p-6 shadow-md hover:shadow-lg transition-all border-2;
  }

  .kpi-card-interactive {
    @apply cursor-pointer hover:scale-105 transform;
  }

  /* Status Badge Styles */
  .status-badge {
    @apply inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold;
  }

  .status-ready {
    @apply bg-emerald-100 text-emerald-800;
  }

  .status-preparing {
    @apply bg-amber-100 text-amber-800;
  }

  .status-served {
    @apply bg-blue-100 text-blue-800;
  }

  .status-online {
    @apply bg-emerald-100 text-emerald-700;
  }

  .status-offline {
    @apply bg-red-100 text-red-700;
  }

  /* Table Styles */
  .table-header {
    @apply bg-gray-50 border-b border-gray-200;
  }

  .table-row {
    @apply border-b border-gray-100 hover:bg-gray-50 transition-colors;
  }

  .table-cell {
    @apply px-6 py-4;
  }

  /* Form Styles */
  .form-group {
    @apply mb-4;
  }

  .form-label {
    @apply block text-sm font-medium text-gray-700 mb-2;
  }

  .form-input {
    @apply w-full px-4 py-2 border border-gray-300 rounded-lg focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 focus:outline-none;
  }

  /* Dropdown/Select Styles */
  .dropdown {
    @apply absolute top-full left-0 mt-2 bg-white border border-gray-200 rounded-lg shadow-lg z-50;
  }

  .dropdown-item {
    @apply px-4 py-2 text-left hover:bg-gray-100 transition-colors text-gray-700;
  }

  .dropdown-item-active {
    @apply bg-[#FF6B00] text-white font-semibold;
  }

  /* Alert/Banner Styles */
  .alert {
    @apply px-4 py-3 rounded-lg text-sm font-medium;
  }

  .alert-info {
    @apply bg-blue-50 text-blue-700 border border-blue-200;
  }

  .alert-success {
    @apply bg-emerald-50 text-emerald-700 border border-emerald-200;
  }

  .alert-warning {
    @apply bg-amber-50 text-amber-700 border border-amber-200;
  }

  .alert-danger {
    @apply bg-red-50 text-red-700 border border-red-200;
  }

  /* Loading States */
  .loading {
    @apply opacity-50 pointer-events-none;
  }

  .skeleton {
    @apply bg-gray-200 rounded animate-pulse;
  }

  /* Utility Classes */
  .truncate-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .truncate-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .smooth-transition {
    @apply transition-all duration-200 ease-in-out;
  }

  .focus-ring {
    @apply focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500;
  }
}

/* Animations */
@layer utilities {
  @keyframes slide-in {
    from {
      opacity: 0;
      transform: translateX(-10px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-slide-in {
    animation: slide-in 0.3s ease-in-out;
  }

  .animate-fade-in {
    animation: fade-in 0.3s ease-in-out;
  }

  .animate-scale-in {
    animation: scale-in 0.3s ease-in-out;
  }
}

/* Scrollbar Customization */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: #f1f5f9;
}

::-webkit-scrollbar-thumb {
  background: #cbd5e1;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #94a3b8;
}

/* Responsive Adjustments */
@media (max-width: 768px) {
  .header-content {
    @apply px-4 py-3;
  }

  .table-cell {
    @apply px-4 py-3 text-xs;
  }

  .card-body {
    @apply p-4;
  }
}

/* Print Styles */
@media print {
  body {
    @apply bg-white;
  }

  .no-print {
    @apply hidden;
  }
}